import dotenv from 'dotenv';

dotenv.config();

export const databaseConfig = {
  config: {
    host: "localhost",
    user: "root",
    password: "satyam@90",
    database: "sourcing",
    port : 3306   
  
       
  }
};
