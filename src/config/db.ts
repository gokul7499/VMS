import dotenv from 'dotenv';

dotenv.config();

export const databaseConfig = {
  config: {
    host: "localhost",
    user: "root",
    password: "pratu8182",
    database: "sourcing",
    port : 3306
  }
};
