import dotenv from 'dotenv';

dotenv.config();

export const databaseConfig = {
  config: {
    host: "localhost",
    user: "root",
    password: "rahul@1866",
    database: "auth",
    port : 3306   
  
  }
};
